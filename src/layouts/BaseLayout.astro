---
import { getLangFromUrl, useTranslations, LANGUAGES } from '../i18n/ui';
import SEOHead from '../components/SEOHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import LanguageBanner from '../components/LanguageBanner.astro';
import '../styles/globals.css';
import '../styles/fonts.css';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogImageAlt?: string;
  ogType?: string;
  schema?: any;
  robots?: string;
  articlePublishedTime?: string;
  articleModifiedTime?: string;
  articleAuthor?: string;
  articleTags?: string[];
  articleSection?: string;
  keywords?: string;
  alternateLangs?: { lang: string; url: string }[];
}

const { title, description, ogImage, ogImageAlt, ogType, schema, robots = 'index, follow', articlePublishedTime, articleModifiedTime, articleAuthor, articleTags, articleSection, keywords, alternateLangs: alternateLangsOverride } = Astro.props;
const lang = getLangFromUrl(Astro.url) as keyof typeof LANGUAGES;
const dir = LANGUAGES[lang]?.dir || 'ltr';
const t = useTranslations(lang);
const canonicalURL = Astro.url.href;

const siteBase = (Astro.site?.href || 'https://iptv4kworld.com').replace(/\/$/, '');
const alternateLangs = alternateLangsOverride ?? Object.keys(LANGUAGES).map((code) => {
  const pathSegments = Astro.url.pathname.split('/').filter(Boolean);
  if (pathSegments.length > 0 && pathSegments[0] in LANGUAGES) {
    pathSegments[0] = code;
  }
  return {
    lang: LANGUAGES[code as keyof typeof LANGUAGES]?.locale || code,
    url: `${siteBase}/${pathSegments.length ? pathSegments.join('/') + '/' : ''}`,
  };
});

// x-default hreflang must always point to the English (default) version
const xDefaultUrl = alternateLangs.find((a) => a.lang === 'en-US')?.url || canonicalURL;

const gaScript = `
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-GV3H6SQGFT');
`;
---

<!doctype html>
<html lang={LANGUAGES[lang]?.locale || 'en'} dir={dir}>
  <head>
    <meta charset="UTF-8" />
    <meta name="theme-color" content="#0f172a" />
    <meta name="color-scheme" content="dark" />
    <SEOHead
      title={title}
      description={description}
      canonicalURL={canonicalURL}
      xDefaultUrl={xDefaultUrl}
      ogImage={ogImage}
      ogImageAlt={ogImageAlt || description}
      ogType={ogType}
      lang={lang}
      alternateLangs={alternateLangs}
      schema={schema}
      robots={robots}
      articlePublishedTime={articlePublishedTime}
      articleModifiedTime={articleModifiedTime || articlePublishedTime}
      articleAuthor={articleAuthor}
      articleTags={articleTags}
      articleSection={articleSection}
      keywords={keywords}
    />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="192x192" href="/favicon.png" />
    <link rel="manifest" href="/manifest.json" />

    <!-- ═══ Font Loading ═══ -->
    <!-- Inter (weights 400-800) is self-hosted via Fontsource — zero external requests -->
    <!-- Noto Naskh Arabic (RTL pages) loaded from Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Naskh+Arabic:wght@400;600;700&display=swap" media="print" onload="this.media='all';this.onload=null" />
    <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Noto+Naskh+Arabic:wght@400;600;700&display=swap" /></noscript>
    <style>
      body { font-display: swap; }
      @font-face {
        font-family: 'Noto-Naskh-Arabic-fallback';
        size-adjust: 105%;
        src: local('Arial');
      }
    </style>

    <!-- DNS prefetch for WhatsApp (used on every page for CTAs) -->
    <link rel="dns-prefetch" href="https://wa.me" />
    <link rel="preconnect" href="https://wa.me" />

    <!-- Google Analytics (async = non-blocking) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-GV3H6SQGFT" is:inline></script>
    <script set:html={gaScript} is:inline></script>
    <noscript>
      <style>
        .reveal, .reveal-left, .reveal-right, .reveal-scale, .reveal-fade {
          opacity: 1 !important;
          transform: none !important;
        }
        #countdown-timer { display: none !important; }
      </style>
    </noscript>
  </head>
  <body class="font-sans">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-3 focus:bg-brand-600 focus:text-white focus:rounded-lg focus:font-semibold focus:outline-none focus:ring-2 focus:ring-brand-400">{t('a11y.skipToContent')}</a>
    <LanguageBanner />
    <Header />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
    <FloatingWhatsApp />
    <script src="/scripts/reveal.js" defer is:inline></script>
    <script src="/scripts/analytics.js" defer is:inline></script>
  </body>
</html>
