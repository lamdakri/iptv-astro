---
import { useTranslations, LANGUAGES } from '../../i18n/ui';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import { WHATSAPP_NUMBER as WA } from '../../i18n/whatsapp';
import WhatsAppIcon from '../../components/icons/WhatsAppIcon.astro';

export function getStaticPaths() {
  return Object.keys(LANGUAGES).map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params;
const t = useTranslations(lang);
const pageTitle = t('privacy.title') + ' | ' + t('site.name');
const pageUrl = Astro.url.href;

const pageSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  name: pageTitle,
  description: t('privacy.subtitle'),
  url: pageUrl,
  inLanguage: lang,
  isPartOf: {
    '@type': 'WebSite',
    name: t('site.name'),
    url: Astro.site?.href || 'https://iptv4kworld.com',
  },
  about: {
    '@type': 'Thing',
    name: 'Privacy Policy',
  },
  lastReviewed: '2026-06-12',
};
---

<BaseLayout title={pageTitle} description={t('privacy.subtitle')} schema={pageSchema}>
  <section class="pt-32 pb-20">
    <div class="container-custom max-w-3xl">
      <Breadcrumb items={[
        { label: t('breadcrumb.home'), href: '/' + lang + '/' },
        { label: t('privacy.title') },
      ]} />

      <h1 class="text-4xl md:text-5xl font-extrabold text-white mb-4 reveal">{t('privacy.title')}</h1>
      <p class="text-surface-400 text-lg mb-2 reveal" data-reveal-delay="100">{t('privacy.subtitle')}</p>
      <p class="text-surface-500 text-sm mb-12 reveal" data-reveal-delay="150">{t('privacy.lastUpdated')}: June 12, 2026</p>

      <div class="space-y-10 reveal" data-reveal-delay="200">
        <p class="text-surface-300 leading-relaxed">{t('privacy.intro')}</p>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.info')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.info.text')}</p>
        </section>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.usage')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.usage.text')}</p>
        </section>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.cookies')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.cookies.text')}</p>
        </section>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.sharing')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.sharing.text')}</p>
        </section>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.rights')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.rights.text')}</p>
        </section>

        <section>
          <h2 class="text-2xl font-bold text-white mb-4">{t('privacy.contact')}</h2>
          <p class="text-surface-400 leading-relaxed">{t('privacy.contact.text')}</p>
          <a
            href={`https://wa.me/${WA}?text=${encodeURIComponent(t('whatsapp.privacyQuestion'))}`}
            class="btn-whatsapp mt-5"
            target="_blank"
            rel="noopener noreferrer"
          >              <WhatsAppIcon class="h-5 w-5" />
            {t('cta.whatsappSupport')}
          </a>
        </section>
      </div>
    </div>
  </section>
</BaseLayout>
